language: generic

services:
  - docker

before_script:
  - scripts/build-aws.sh

script:
  - scripts/test-aws.sh

deploy:
  provider: script
  script: bash scripts/deploy-aws.sh
  on:
    branch: master